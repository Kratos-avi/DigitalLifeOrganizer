# JWT
JWT_SECRET=replace-with-strong-secret

# Option A (recommended for Railway): full MySQL URL
# Example: mysql://user:password@host:port/railway
MYSQL_URL=

# Option B: split values (use if MYSQL_URL is empty)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=digital_life_organizer
DB_CONNECTION_LIMIT=10
